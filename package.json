{
  "name": "netflix-clone",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "bootstrap": "npx lerna bootstrap --hoist",
    "dev": "lerna run dev --parallel",
    "build": "npx lerna run build --scope common --scope web",
    "storybook": "lerna run storybook --parallel",
    "clean": "lerna run clean && rm -rf node_modules && rm -rf package-lock.json"
  },
  "author": "Milos Andrejic",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.4.1",
    "eslint-plugin-babel": "^5.3.1",
    "eslint-plugin-react": "^7.27.1",
    "lerna": "^4.0.0"
  }
}
